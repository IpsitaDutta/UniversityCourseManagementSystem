html
    body
        h1 #{appName}
        h2 Register Courses
        form(action="/joincourse" method="post")
            label Course Name
            input(type="text" name="course")
            input(type="submit")